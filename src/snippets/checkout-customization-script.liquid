{% comment %}
	:: UK / EU : Limit Address fields to 35 characters ::
		- There is an issue with the EDI File handoff that gives order to warehouse that truncates
		  address field to 35 characters, causing broken ship addresses. Not requested for US / CA.
  :: PDM-259 : Limite first and last name fields to 35 chars also ::
{% endcomment %}
{% unless settings.international_store_version == "US" or settings.international_store_version == "CA" %}
	<script type="text/javascript">
    var  fname = document.getElementById( 'checkout_shipping_address_first_name' ),
         lname = document.getElementById( 'checkout_shipping_address_last_name' ),
      address1 = document.getElementById( 'checkout_shipping_address_address1' ),
      address2 = document.getElementById( 'checkout_shipping_address_address2' ),
      maxChars = 35; // Character Max

		function setMax( el ) {
			if ( el ) {
				el.maxLength = maxChars; // Character Count Limit
			}
		}

		var maxList = [ fname, lname, address1, address2 ];
		maxList.forEach( function( el ) {
			setMax( el );
		});

	</script>
{% endunless %}
